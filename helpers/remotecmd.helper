#!/bin/bash
args=(ssh)

if [[ ! -z "$THEOS_DEVICE_PORT" ]]; then
	args+=(-p "$THEOS_DEVICE_PORT")
fi

if [ -z "$THEOS_DEVICE_IP" ]; then
	echo "ERROR! The variable THEOS_DEVICE_IP is not set!"
	exit
fi

args+=(-l "root")

args+=($LIME_IP)
args+=("$@")

exec ${args[@]}
